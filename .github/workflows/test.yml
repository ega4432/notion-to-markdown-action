name: 'build-test'

on: # rebuild any PRs and main branch changes
  pull_request:

jobs:
  build: # make sure build/ci work properly
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}

      - name: Set Node.js 16.x
        uses: actions/setup-node@v3
        with:
          node-version-file: .tool-versions
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - run: npm install

      - run: npm run all # exec: [ prettier, eslint, test, build, package ]

  normal-test: # make sure the action works on a clean machine without building
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}

      - run: |
          pwd
          ls -l
          ls -1 | wc -l

      - uses: ./
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          ACTIONS_STEP_DEBUG: true
          ACTIONS_RUNNER_DEBUG: true

      - run: |
          pwd
          ls -l output
          ls -1 output | wc -l
          ls -l /output

  output-path-test: # make sure the action works on a clean machine without building
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}

      - run: |
          pwd
          ls -l
          ls -1 | wc -l

      - uses: ./
        with:
          output_path: some/dir
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          ACTIONS_STEP_DEBUG: true
          ACTIONS_RUNNER_DEBUG: true

      - run: |
          pwd
          ls -l some/dir/
          ls -1 some/dir/ | wc -l
          ls -l /some/dir/

  # filename-prop-test: # make sure the action works on a clean machine without building
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 1
  #   steps:
  #     - name: Checkout source
  #       uses: actions/checkout@v3
  #       with:
  #         ref: ${{ github.head_ref }}

  #     - name: Test
  #       id: filename_prop
  #       uses: ./
  #       with:
  #         filename_property: 'slug'
  #       env:
  #         NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
  #         NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
  #         ACTIONS_STEP_DEBUG: true
  #         ACTIONS_RUNNER_DEBUG: true

  #    TODO:
  #    - name: Test specified filename prop
  #      if: ${{ steps.filename_prop.outcome == 'success' }}
  #      run: |
  #       ls -l
